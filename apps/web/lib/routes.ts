/* The authenticated home. The Manager conversation IS the main screen — login
   and Crew Genesis both land here — while /command keeps the Command Center
   dashboard at its original URL so every existing deep link still works. */
export const HOME_PATH = "/manager" as const;
