# Moseisley.sh backend dependencies (Python >= 3.11)
fastapi>=0.115,<1
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0,<2.1
alembic>=1.13
aiosqlite>=0.20
asyncpg>=0.29
pydantic>=2.7,<3
pydantic-settings>=2.2
httpx>=0.27
cryptography>=42
PyJWT>=2.8
python-multipart>=0.0.9
email-validator>=2.1
fastapi-users[sqlalchemy]>=14,<16
aiosmtplib>=3.0
boto3>=1.34

# Private per-user EOD equity/ETF data (assistant mode only — never rendered on a
# public surface, never redistributed). Pulls pandas+numpy: ~192MB installed.
yfinance>=0.2

# development / testing
pytest>=8
pytest-asyncio>=0.23
ruff>=0.5
